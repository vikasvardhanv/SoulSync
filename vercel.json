{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install && npm run build-frontend",
  "buildCommand": "npm run build --workspace=backend",
  "outputDirectory": "frontend/dist"
}