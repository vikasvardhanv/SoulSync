# Force npm to install optional dependencies to fix Rollup native binaries
optional=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Enable fund messages
fund=false
# Set audit level
audit-level=moderate